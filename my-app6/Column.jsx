const ______ = ({ width, style, title, content }) => {
  const widthStyle = `column is-${_____} ${_____} `;
  return (
    <div class={__________}>
      <section class="section">
        <div class="container">
          <h1 class="title">{title}</h1>
          <h2 class="subtitle">{content}</h2>
        </div>
      </section>
    </div>
  );
};

export default ______;
